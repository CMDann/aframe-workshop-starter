# A-Frame Cheat Sheet

> **Coming in Phase 4.** This will be a one-page, print-friendly quick reference covering exactly the primitives and attributes used in the starter template — `a-box`, `a-sphere`, `a-plane`, `a-cylinder`, `a-sky`, `a-light`, and `a-entity` with `gltf-model` — each with a copy-pasteable example line, plus the `position` / `rotation` / `scale` / `color` syntax.
>
> Scoped to what this workshop actually uses, not a full A-Frame reference.

**In the meantime:** the starter template at [../starter-template/index.html](../starter-template/index.html) is commented to serve the same purpose, and the official docs at <https://aframe.io/docs/> cover everything beyond this workshop's scope.
